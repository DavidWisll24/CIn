/*

void setup() {
  Serial.begin(9600);
  float soma = 0;
  float media = 0;
  int num[] = {4, 8, 4, 2, 4};

  for (int i = 0; i < 5; i++){
    soma += num[i];
  }

  int tamanho = sizeof(num)/sizeof(num[0]);
  media = soma / tamanho;

  Serial.println(soma);
  Serial.println(media);
}

void loop() {

}

*/